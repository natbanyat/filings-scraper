# 2026-04-28

- Patched `scripts/official_doc_corpus.py` with a `repair` command to mark stale `runs` as failed and requeue stale running parse jobs, and wired that repair step into both scrape startup and parse draining.
- Added repo-managed systemd scaffolding for the official docs stack: `systemd/openclaw-docs-ui.service`, `systemd/openclaw-docs-parse-drain.service`, and `systemd/openclaw-docs-parse-drain.timer`, plus updated `install-ui-service.sh` to install/enable them together.
- Applied the live repair: marked 3 stale PRU runs failed, drained the remaining 17 pending parse jobs successfully, and restarted the local docs UI on `127.0.0.1:8876`.
