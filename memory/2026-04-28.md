# 2026-04-28

- Stabilized official docs corpus runtime: added stale-run repair flow, parse-drain systemd timer, and fixed docs UI handoff to systemd.
- Diagnosed PSIX zero-doc corpus run: root cause was missing `sec_cik` plus Python `requests` timeouts against `investors.psiengines.com` IR pages.
- Patched `scripts/custom_tickers.json` for `custom/psix` to use SEC CIK `1137091` and rely on SEC discovery rather than the timing-out IR site.
- Verified a fresh PSIX incremental run completed with 20 discovered/downloaded docs under `PSIX_power-solutions-international-inc`.
