# 2026-04-17

- Implemented the missing inbox handoff: material daily briefs now write markdown files to `/mnt/c/Users/natba/OneDrive/@ Cowork/investing/inbox` using the downstream processor schema (`type`, `ticker`, `tickers_mentioned`, `source`, `priority`, `tags`, `status`, `date`).
- Added `scripts/inbox_writer.py` and hooked it into `daily_news.py` and `macro_close.py` before Discord posting, so inbox delivery still happens if posting/rendering fails.
- Fixed a live robustness bug in `post_discord.py`: `build_embed()` now tolerates list-shaped model output for `headline` / `what_changed` / `key_debate` fields instead of crashing on `.strip()`.
- Updated `TOOLS.md` so the documented inbox schema matches the processor that Nat expects, and pointed delivery checks to `logs/daily_news.log` / `logs/macro_close.log`.
- Expanded active coverage config to include HSBC (Asia HK close), MUFG (Asia Japan close), Mizuho via `MFG` (Asia Japan close), and `markets/korea` for EWY-style Korea market monitoring.
- Added inbox-only watchlist routing in `daily_news.py`: PRU, MQG, GDG, CGF, HKEX, Kyoto FG, Resona, Chiba Bank, Daiwa House, Mitsubishi Estate, ERSTE, HANG-LUNG, and IBKR now get a lightweight material-news scan by close window and write `type: note` inbox files when something material is flagged.
- Added specificity hints / metadata / RSS matching for HSBC, MUFG, Mizuho, and Korea market coverage, plus stub coverage folders for HSBC, MUFG, and MFG.
- Remapped Discord routing so HSBC posts into the existing STAN channel, and MUFG + Mizuho (`MFG`) post into the existing 8316/SMFG channel by updating both `scripts/config.py` and `scripts/channel_map.json`.
