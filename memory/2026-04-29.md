# 2026-04-29

- Patched `scripts/openclaw_gateway_model.py` defaults to use `openai-codex/gpt-5.4` instead of disallowed `openai/gpt-5.5` family refs; committed as `8581a92`.
- Verified wrapper path directly: `run_text('Reply with exactly: OK')` returned `OK` with `DEFAULT_MODEL=openai-codex/gpt-5.4`.
- Direct smoke reruns after patch:
  - `catalyst_monitor.py` completed cleanly and posted 6 catalyst alerts.
  - `daily_news.py us` completed cleanly, wrote new inbox files with `status: inbox`, and posted covered-name updates.
- `openclaw cron list --all` on this gateway showed only 4 unrelated jobs; expected cron jobs `macro_close`, `daily_news`, `breaking_news`, `catalyst_monitor` were absent, so UUID-based cron validation remains blocked on scheduler state rather than the Python pipeline.
- `openclaw tasks audit --severity error` showed stale-running task findings but no `credit balance` text and no new provider/auth/rate-limit/response-shape errors.
