# 2026-04-15

- Added standing instruction for material covered ticker, sector, and macro updates: write one markdown inbox file per ticker/story to `C:\Users\natba\OneDrive\@ Cowork\investing\inbox` (`/mnt/c/Users/natba/OneDrive/@ Cowork/investing/inbox` in WSL) with the exact YAML front matter, `status: inbox`, and the filename format `YYYY-MM-DD_TICKER_type_brief-description.md`.
- Confirmed the inbox directory exists.
- Wrote and verified a test markdown file in the inbox path: `/mnt/c/Users/natba/OneDrive/@ Cowork/investing/inbox/2026-04-15_MACRO_commentary_inbox-write-test.md`.
- Fixed a daily news pipeline bug where broad RSS feeds could leak unrelated stories into single-name updates. Added per-ticker RSS alias matching so items like Morgan Stanley earnings no longer enter the SE pipeline unless SE aliases/brands are mentioned.
- Hardened sector pipelines after a bad `korea-memory` post: added RSS alias matching for sector feeds, added a Korea-memory specificity rule in Pass 1, mapped Korea-memory into portfolio sector context extraction, and raised Pass 2 token budget to reduce truncated fallback posts.
- Broader audit found systemic Pass 2 truncation across multiple names, not just SE or Korea-memory. Added a compact Pass 2 retry path that re-runs with fewer articles and a terser schema when the first synthesis returns truncated/non-parseable JSON.
- Hardened `sectors/exchanges` after another bad post: tightened exchange-sector RSS match terms, added an exchange-specific Pass 1 relevance rule, and blocked `manilatimes.net` / `indiatimes.com` from the pipeline as low-signal sources.
- Added a posting guardrail in `daily_news.py`: if Pass 2 still falls back to the generic 'synthesized brief unavailable' stub after retries, suppress the Discord post entirely instead of shipping a junk brief.
