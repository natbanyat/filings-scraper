# 2026-04-24

- Diagnosed the official-doc parser backlog. Root cause: docs UI `api_run` dispatched `official_doc_corpus.py run` without `--parse`, so new scrapes queued parse jobs but did not process them unless parse backfill was triggered separately.
- Current corpus state when checked: 160 pending parse jobs, no parser worker process active, and no `openclaw-docs-ui` systemd unit installed.
- Fixed `council_ui/official_docs_server.py` so UI-triggered scrapes now append `--parse` by default and updated the UI copy/message to reflect inline parsing.
- Started a manual parser drain with `scripts/official_doc_corpus.py parse --limit 500` and started the local docs UI server on `127.0.0.1:8876`.
- Noted a secondary issue in the job queue: some legacy STAN parse jobs fail with `document record not found`, which looks like stale orphaned jobs rather than the parser itself being broken.
