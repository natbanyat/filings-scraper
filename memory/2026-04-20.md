# 2026-04-20

- Researched whether X's official API can replace the current `twitter_signal.py` vendor feed.
- Conclusion: yes, the best first migration is official X user timeline polling (`GET /2/users/by/username/:username` + `GET /2/users/:id/tweets`) while keeping the current scheduled windows, LLM triage, and Discord posting logic intact.
- Drafted migration plan in `research/architecture/twitter-signal-x-api-migration.md`.
- Key implementation note: the current 68-handle watchlist is too large for a single recent-search query and too large for a single filtered-stream rule, so search/stream should not be the first migration path.
